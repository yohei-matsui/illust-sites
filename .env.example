# Vercel の環境変数(Project Settings → Environment Variables)に設定する値
WORKER_URL=https://<workspace>--illust-gen-web.modal.run
WORKER_TOKEN=<Modal の Secret "illust-gen" に入れた WORKER_TOKEN と同じ値>
# 任意: 設定すると生成ページの利用にキーが必要になります(GPU 代の保護用)
SITE_ACCESS_KEY=
